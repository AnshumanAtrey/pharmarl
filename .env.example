# Copy to .env and fill in. .env is gitignored — never commit secrets.

# Hugging Face / deployment
# Required for deploying to HF Spaces and pushing trained adapters to the Hub.
# Get a write-scoped token at https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_write_scoped_token_here
HF_USERNAME=your-hf-username
HF_SPACE_NAME=pharmarl

# Training / Kaggle
# Set PHARMARL_ENV_URL to your deployed Space URL when training from Kaggle.
# Use /kaggle/working/... paths in notebook/script args for outputs/checkpoints.
PHARMARL_ENV_URL=https://your-hf-username-pharmarl.hf.space

# Optional tracking (Weights & Biases)
# WANDB_API_KEY=your_wandb_api_key_here
WANDB_PROJECT=pharmarl
WANDB_RUN_NAME=kaggle-grpo-run

# Required only if you'll run inference.py (uses the OpenAI client). Costs money.
# OPENAI_API_KEY=sk-...

# Required only if you'll run examples/gemini_episode.py.
# Get a free or paid key at https://aistudio.google.com/apikey
# GEMINI_API_KEY=AIza...

# Required only if you'll run examples/openrouter_episode.py (Llama, Mistral, etc.).
# Sign up at https://openrouter.ai/keys
# OPENROUTER_API_KEY=sk-or-v1-...

# Optional — opt into Stage 2 docking (requires pyscreener + Vina + OpenBabel).
# PHARMARL_ENABLE_DOCKING=1

# ─── UI live agents (ui/app.py) ─────────────────────────────────────────
# Two patterns supported per agent. Set EITHER:
#
#   (a) OpenAI-compatible:  *_BASE_URL + *_API_KEY + *_MODEL
#       Works with Together AI, vLLM serve, OpenRouter, Groq, HF Inference
#       Endpoints (paid, dedicated GPU per model).
#
#   (b) HF Inference Providers:  *_HF_PROVIDER + *_MODEL
#       Routes through HF's unified gateway using HF_TOKEN above.
#       Provider values: hf-inference, together, replicate, fal-ai, novita, etc.
#
# Pre-training baseline — Llama 3.2 3B via your provider
# PRETRAINED_BASE_URL=https://api.together.xyz/v1
# PRETRAINED_API_KEY=tgp_v1_...
# PRETRAINED_MODEL=meta-llama/Llama-3.2-3B-Instruct-Turbo
# PRETRAINED_TEMPERATURE=0.3
# # — or via HF Inference Providers —
# PRETRAINED_HF_PROVIDER=hf-inference
# PRETRAINED_MODEL=meta-llama/Llama-3.2-3B-Instruct
#
# Post-GRPO adapter (typically your dedicated HF Inference Endpoint)
# POSTTRAINED_BASE_URL=https://xxx.us-east-1.aws.endpoints.huggingface.cloud/v1
# POSTTRAINED_API_KEY=hf_your_endpoint_token
# POSTTRAINED_MODEL=tgi
# POSTTRAINED_TEMPERATURE=0.3
